# Generated MP3 files live here after running `npm run generate-audio`.
# Filenames are content hashes of the Romanian text being spoken; the
# manifest at src/data/audio-manifest.ts maps text → filename.
